{
  "name": "elfix",
  "version": "1.29.0",
  "description": "ELFIX, a compact Chromium-based streaming website browser.",
  "author": "RoveHD",
  "repository": {
    "type": "git",
    "url": "https://github.com/RoveHD/elfix.git"
  },
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "typecheck": "node --check shared/provider-model.js && node --check shared/bildausschnitt.js && node --check src/main.js && node --check src/adblock-engine.js && node --check src/adblock-kosmetik.js && node --check src/verifizierungstor.js && node --check src/youtube.js && node --check src/discover.js && node --check src/taste.js && node --check src/titel.js && node --check src/empfehlung.js && node --check src/metadaten.js && node --check src/statistik.js && node --check src/voe-qualitaet.js && node --check src/watchparty.js && node --check src/watchparty-raeume.js && node --check src/watchparty-sync.js && node --check src/youtube-sync.js && node --check src/youtube-watchparty.js && node --check src/sicherung.js && node --check src/preload.js && node --check src/renderer/renderer.js && node --check src/begruendung.js",
    "lint": "node scripts/lint.js",
    "test": "npm run typecheck && npm run suite",
    "make-icon": "node scripts/make-icon.js",
    "dist": "npm run make-icon && electron-builder --win nsis portable",
    "dist:installer": "npm run make-icon && electron-builder --win nsis",
    "publish:github": "npm run make-icon && electron-builder --win nsis portable --publish always",
    "suite": "node tests/run.js"
  },
  "build": {
    "appId": "com.rovehd.elfix",
    "productName": "ELFIX",
    "directories": {
      "output": "dist"
    },
    "files": [
      "shared/**/*",
      "src/**/*",
      "build/icon.ico",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "build/icon.ico"
    },
    "nsis": {
      "artifactName": "ELFIX-Setup-${version}-${arch}.${ext}",
      "oneClick": true,
      "perMachine": false,
      "allowElevation": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "ELFIX",
      "deleteAppDataOnUninstall": false
    },
    "portable": {
      "artifactName": "ELFIX-Portable-${version}-${arch}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "RoveHD",
        "repo": "elfix",
        "releaseType": "release"
      }
    ],
    "icon": "build/icon.ico"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "@adguard/tsurlfilter": "^6.0.2",
    "electron-updater": "^6.8.9"
  }
}
